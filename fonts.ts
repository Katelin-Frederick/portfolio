import { Oxygen_Mono, Montserrat, } from 'next/font/google'

export const oxygen = Oxygen_Mono({
  weight: ['400'],
  subsets: ['latin'],
  display: 'swap',
})

export const montserrat = Montserrat({
  subsets: ['latin'],
  display: 'swap',
})